// Command scaffold instantiates the ./template module into a brand-new Go CLI
// project: it copies template/, replaces the sentinel tokens, renames the
// cmd/<tool> directory, and runs `go mod tidy` + `git init`.
//
// This is a build placeholder. The real implementation is tracked in beads
// issue go_cli_tool_template-enz (F11) and golden tests in -7px (F12).
package main

func main() {}
