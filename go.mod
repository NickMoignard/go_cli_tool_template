// Root module: hosts cmd/scaffold, the deterministic instantiation tool (ADR-0003).
// The Template lives in ./template (its own module, sentinel path) and the worked
// example in ./examples/yaml-validator (its own module). See ADR-0001.
module github.com/NickMoignard/go_cli_tool_template

go 1.25
