# With no arguments the tool prints help to stdout and exits 0.
exec yamlvalidate
stdout 'USAGE'
! stderr .

# --help is the same, explicitly requested.
exec yamlvalidate --help
stdout 'USAGE'
! stderr .
